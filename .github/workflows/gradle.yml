name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up jdk8
      uses: actions/setup-java@v1
      with:
        java-version: jdk8fx
    - name: Build with Gradle
      run: ./gradlew build
